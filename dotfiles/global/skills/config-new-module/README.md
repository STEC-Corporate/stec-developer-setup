# config-new-module

Criação de módulo fullstack (package + backend + frontend) via script gerador idempotente.

## Origem
Inspirado na skill `config-new-module` do `projeto-ai` (`skills-ddd-clean`).

