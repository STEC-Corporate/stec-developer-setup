# core-repository

Padrão de repositórios (interfaces no domínio, implementações na infraestrutura) com mapeamento Entity↔Record.

## Origem
Inspirado na skill `core-repository` do `projeto-ai` (`skills-ddd-clean`).

