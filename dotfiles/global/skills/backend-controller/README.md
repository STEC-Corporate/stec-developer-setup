# backend-controller

Controllers NestJS alinhados a Clean Architecture (DTO + Swagger + delegação a use cases).

## Origem
Inspirado na skill `backend-controller` do `projeto-ai` (`skills-ddd-clean`).

