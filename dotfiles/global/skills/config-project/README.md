# config-project

Bootstrap/reconciliação idempotente de um monorepo fullstack (apps/backend, apps/web, packages/*).

## Origem
Inspirado na skill `config-project` do `projeto-ai` (`skills-ddd-clean`).

